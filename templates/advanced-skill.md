---
name: advanced-skill-name
version: 1.0.0
description: 高级 Skill 模板，包含完整配置项
author: your-name
tags: [tag1, tag2]
dependencies:
  - other-skill@^1.0
  - helper-skill@^2.0
---

# Skill 名称

## 概述
[简要描述 Skill 的目的和价值]

## 触发条件

### 显式触发
| 方式 | 触发器 | 说明 |
|------|--------|------|
| 命令 | `/command-name` | 直接调用 |
| 别名 | `/alias` | 简短别名 |
| 关键词 | "关键词1", "关键词2" | 自然语言触发 |

### 条件触发
```yaml
conditions:
  - type: file_detected
    pattern: "*.csv"
    action: suggest  # suggest | auto_execute
  - type: context_match
    keywords: ["数据分析"]
    action: suggest
```

## 上下文要求

### 必需参数
| 参数 | 类型 | 说明 | 验证规则 |
|------|------|------|----------|
| input_source | string | 输入数据源 | 非空 |
| format | enum | 数据格式 | csv/json/excel |

### 可选参数
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| verbose | boolean | false | 详细输出 |
| max_rows | number | 10000 | 最大处理行数 |

### 环境要求
- [ ] 需要网络访问
- [x] 需要文件系统访问
- [ ] 需要数据库连接

## 执行逻辑

### 流程图
```
┌─────────┐    ┌─────────┐    ┌─────────┐
│  输入   │───►│  处理   │───►│  输出   │
└─────────┘    └─────────┘    └─────────┘
                   │
              ┌────┴────┐
              │ 验证&约束 │
              └─────────┘
```

### Step 1: 输入验证
**目标**：验证输入参数的有效性

**操作**：
1. 检查必需参数完整性
2. 验证参数格式
3. 确认资源可访问性

**错误处理**：
- 参数缺失 → 提示用户补充
- 格式错误 → 说明正确格式
- 资源不可用 → 建议替代方案

### Step 2: 核心处理
**目标**：执行主要业务逻辑

**操作**：
1. 子操作1
2. 子操作2
3. 子操作3

**中间状态**：
```json
{
  "progress": 50,
  "current_step": "processing",
  "intermediate_result": {}
}
```

### Step 3: 结果生成
**目标**：格式化输出结果

**操作**：
1. 整理处理结果
2. 应用输出格式
3. 质量检查

## 输出规范

### 主输出
**格式**：Markdown

**模板**：
```markdown
# [标题]

## 摘要
[简要总结]

## 详情
[详细内容]

## 建议
[下一步建议]
```

### 附加输出
| 输出项 | 格式 | 条件 |
|--------|------|------|
| 详细日志 | text | verbose=true |
| 数据文件 | csv | export=true |

## 约束条件

### 功能边界
| 约束项 | 限制值 | 说明 |
|--------|--------|------|
| 最大数据量 | 100MB | 超出需分批 |
| 超时时间 | 300s | 超时自动终止 |
| 并发限制 | 1 | 不支持并发 |

### 安全约束
- [ ] 不访问外部网络
- [x] 不修改源数据
- [x] 敏感信息脱敏

### 质量约束
- 输出必须可验证
- 不确定内容明确标注
- 保持输出格式一致

## 错误处理

### 错误码
| 错误码 | 说明 | 用户提示 |
|--------|------|----------|
| E001 | 输入无效 | 请检查输入参数 |
| E002 | 处理超时 | 数据量过大，请分批处理 |
| E003 | 格式不支持 | 仅支持 csv/json/excel 格式 |

### 降级策略
当无法完成完整处理时：
1. 返回已处理部分
2. 说明未完成原因
3. 建议后续操作

## 依赖调用

### 调用其他 Skill
```markdown
在 Step 2 中调用：
- `@skill/data-validation` 进行数据校验
- `@skill/format-converter` 进行格式转换
```

### 被调用说明
本 Skill 可被以下场景调用：
- 作为数据处理流水线的一环
- 作为报告生成的前置步骤

## 测试用例

### TC01: 正常流程
- **输入**：标准 CSV 文件
- **预期**：生成完整报告
- **验证点**：报告包含所有必需章节

### TC02: 边界条件
- **输入**：空文件
- **预期**：返回错误提示
- **验证点**：提示信息清晰

### TC03: 异常处理
- **输入**：损坏的文件
- **预期**：优雅降级
- **验证点**：不崩溃，给出建议

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0.0 | 2024-01-15 | 初始版本 |

## 参考资源
- [相关文档1](link)
- [相关文档2](link)
